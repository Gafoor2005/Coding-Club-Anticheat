<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Coding_Club_Anticheat.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Coding_Club_Anticheat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Coding Club Anticheat">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Custom Title Bar -->
        <Border x:Name="AppTitleBar" 
                Grid.Row="0" 
                Height="48"
                Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- App Icon and Title -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal" 
                            Spacing="12" 
                            VerticalAlignment="Center" 
                            Margin="16,0,0,0">
                    <Border Width="24" 
                            Height="24" 
                            CornerRadius="4" 
                            Background="{ThemeResource AccentFillColorDefaultBrush}">
                        <TextBlock Text="🛡️" 
                                   FontSize="14" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock x:Name="AppTitle" 
                               Text="Coding Club Anticheat" 
                               FontWeight="SemiBold" 
                               FontSize="14"
                               VerticalAlignment="Center" 
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Navigation View -->
        <NavigationView x:Name="MainNavigationView" 
                        Grid.Row="1"
                        SelectionChanged="NavigationView_SelectionChanged"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        OpenPaneLength="280"
                        CompactModeThresholdWidth="640"
                        ExpandedModeThresholdWidth="1280"
                        PaneDisplayMode="Auto">
            
            <!-- Menu Items -->
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="HomePage"
                                    IsSelected="True">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                    <TextBlock Text="Student Mode" VerticalAlignment="Center"/>
                     
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="AdminPanelItem"
                                    Tag="CreateTestPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;"/>
                    </NavigationViewItem.Icon>
                    <TextBlock Text="Admin Panel" VerticalAlignment="Center"/>
                     
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <!-- Footer Menu Items -->
            <NavigationView.FooterMenuItems>
                <!-- Not Signed In State -->
                <NavigationViewItem x:Name="SignInNavItem"
                                    Visibility="Visible"
                                    Tapped="SignInNavItem_Tapped"
                                    Content="Sign In">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE77B;"/>
                    </NavigationViewItem.Icon>
                    
                </NavigationViewItem>

                <!-- Signed In State - User Profile -->
                <NavigationViewItem x:Name="UserNavItem"
                                    Visibility="Collapsed">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE77B;"/>
                    </NavigationViewItem.Icon>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="20" 
                                Height="20" 
                                CornerRadius="10" 
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="UserInitials" 
                                       Text="U" 
                                       FontSize="8" 
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       Foreground="White"/>
                        </Border>
                        <TextBlock x:Name="UserNameText" 
                                   Text="User Name" 
                                   FontSize="14"
                                   VerticalAlignment="Center" 
                                   Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </StackPanel>
                </NavigationViewItem>

                <!-- Signed In State - Sign Out Button -->
                <NavigationViewItem x:Name="SignOutNavItem"
                                    Visibility="Collapsed"
                                    Tapped="SignOutNavItem_Tapped">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7E8;"/>
                    </NavigationViewItem.Icon>
                    <TextBlock Text="Sign Out" VerticalAlignment="Center"/>
                </NavigationViewItem>

                <NavigationViewItemSeparator x:Name="AuthSeparator" 
                                             Visibility="Collapsed"/>

                <NavigationViewItem Content="About" 
                                    Tag="AboutPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame" 
                   Margin="0"/>
            
        </NavigationView>
    </Grid>
</Window>
